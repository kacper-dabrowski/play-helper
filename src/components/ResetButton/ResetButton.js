import React from "react";
import { StyledResetButton } from "./StyledResetButton";

const ResetButton = (props) => (
<StyledResetButton {...props}>Resetuj hasło</StyledResetButton>
);

export default ResetButton;
