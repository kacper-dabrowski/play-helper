import styled from '@emotion/styled';

export const BaseButton = styled.button`
    font-size: 1.1rem;
    height: 2.5rem;
    border: none;
    border-radius: 20px;
    transition: ease-in-out 0.2s;

    &:hover {
        cursor: pointer;
    }
`;
