import React from "react";
import { StyledLoginButton } from "./StyledLoginButton";


const LoginButton = (props) => (
  <StyledLoginButton {...props}>Zaloguj się</StyledLoginButton>
);

export default LoginButton;
