import styled from '@emotion/styled';
import publicIcon from '../../../assets/icons/planet-earth.svg';

export const PublicIcon = styled.div`
    width: 1.5rem;
    height: 1.5rem;
    background-image: url('${publicIcon}');
    background-size: cover;
    position: absolute;
    top: 1rem;
    left: 1rem;
    border: none;
    color: transparent;
`;
