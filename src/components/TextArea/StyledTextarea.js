import styled from 'styled-components';

export const StyledTextArea = styled.textarea`
    border-radius: 10px;
    width: 40%;
    resize: none;
    height: 10rem;
    padding: 0.5rem;
    font: inherit;
`;
