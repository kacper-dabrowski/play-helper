import React from "react";
import { StyledSignupSubmitButton } from "./StyledSignupSubmitButton";

const SignupSubmitButton = () => {
  return <StyledSignupSubmitButton>Zarejestruj</StyledSignupSubmitButton>;
};

export default SignupSubmitButton;
